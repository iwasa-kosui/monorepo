{
  "name": "talks",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev dist --port 8788",
    "build": "node scripts/build.mjs",
    "deploy:prod": "wrangler pages deploy dist"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "wrangler": "^4.20.0"
  }
}
