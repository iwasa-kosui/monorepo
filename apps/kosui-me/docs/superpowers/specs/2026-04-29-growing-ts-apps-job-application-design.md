# 育てるTypeScriptアプリケーション 第1冊「壊れやすいシステムを育てる」設計書

## 概要

kosui.me 上に、サーバサイドTypeScriptの学習教材シリーズ「育てるTypeScriptアプリケーション」を新設する。第1冊として「壊れやすいシステムを育てる: 求人応募システムで学ぶサーバサイドTypeScript」を執筆する。

形式は chibivue を参考にした章立て + サイドバーの教材で、`posts` とは独立した `books` コンテンツコレクションを kosui-me 内に追加する。コード本体は別リポジトリで管理し、各章の開始/終了状態を Git tag で固定して読者が手元で動かせるようにする。

## 教材コンセプト

「ひとつの動くアプリに、要件が次々と降ってきて、雑な実装が変更耐性を失っていく。それをリファクタしながら原則を獲得する」というリファクタ駆動の学習教材。chibivue が「ライブラリを下のレイヤーから積み上げる」型を取るのに対し、本書は「動くアプリを段階的に育てる」型を取る。

教えたい原則は `functional-ts-principles` スキルプラグインに既に集約されているが、それを読み物として伝えるには「なぜそれが必要か」が腹落ちしにくい。本書は要件追加によって痛みを発生させ、原則を「降ってきた要件への応答」として導入することで、原則の必要性を身体に覚えさせる。

## 対象読者

- 他言語（Python / Java / Go / Elixir など）の経験を持つ
- TypeScript 自体は何らかの形で書いた経験がある（フロントエンドなど）
- サーバサイドTypeScriptの本番環境レベルでの実践に不安を持つ

KAKEHASHI Tech Blog の「サーバーサイドTypeScriptの型システムをどう教えるか」（2026年3月公開）が論じた「型検査が保証する範囲とランタイムで担保すべき範囲の境界」を、教える側ではなく**学ぶ側が手を動かして気づく**ための教材として位置づける。

## 約束（What this book promises）

> 本書は、他言語の経験を持ちながらサーバサイドTypeScriptの本番環境レベルでの実践に不安を持つエンジニアに向けて、ひとつの求人応募管理アプリを段階的にリファクタしながら、関数型ドメインモデリングの原則を**なぜそれが必要か**から腹落ちさせる教材です。
>
> 完走すると、Discriminated Union による状態モデリング、Result 型と Railway Oriented Programming、Zod による境界防御、Sensitive 型による PII 保護といった原則を、降ってきた要件に対する応答として自分の手で書けるようになります。読了後、`functional-ts-principles` スキルプラグインを導入することで、その原則をエージェント時代にも継続適用できます。

## シリーズ名と書名

- シリーズ名: **育てるTypeScriptアプリケーション**
- 第1冊書名: **壊れやすいシステムを育てる: 求人応募システムで学ぶサーバサイドTypeScript**
- 識別子: `growing-ts-apps` / `job-application`

シリーズ名を分けることで、Phase 2 や別題材の冊（例: 注文・予約・SaaS 招待など）を将来追加できる構造を確保する。

## 媒体・形式

### 配信先

kosui.me ドメイン内に配信する。`posts` とは独立した content collection `books` を新設し、サイドバー付きの2カラムレイアウトで読ませる。

URL構造案:

- `https://kosui.me/books/` — シリーズ・冊一覧
- `https://kosui.me/books/job-application/` — 第1冊のトップ（はじめに・目次・読了目安）
- `https://kosui.me/books/job-application/00-broken-app/` — 第0章
- `https://kosui.me/books/job-application/01-state-modeling/` — 第1章
- 以下同様

### コンテンツコレクション

Astro の content collection で `books` を新設する。冊単位のメタ情報（タイトル・著者・更新日・全章一覧）と、章単位のメタ情報（章番号・章タイトル・要旨・対応コードタグ）を扱う。MDX で本文を書き、CodePlayground など既存コンポーネントを再利用する。

### サイドバー

冊全体の章一覧を常時表示する。現在地のハイライト、未読/読了の区別、進捗の保存（localStorage）は将来拡張として残す（初版では単純な章リンクのみ）。

## Phase 1 章立て

第0章から第6章 + 最終章の8章構成。第7章（ドメインイベント）と第8章（レイヤードアーキテクチャ）は Phase 2 として将来の拡張余地に残す。

### 第0章: 動くけれど壊れやすいアプリ

- 求人応募の最低限のCRUDを持つアプリ
- 単一の `JobApplication` 型に `status: string` と全フィールドを詰め込む
- `throw` だらけ、`as JobApplication` だらけ、巨大関数
- ロガーに応募者をまるごと渡している
- **読者の状態**: 「これは普通に動くな」と思える

### 第1章: 「不採用にした応募が、なぜか面接予定に戻った」

- 降ってくる要件: 不正な状態遷移を防ぎたい
- 引き出す原則: Discriminated Union による状態モデリング、純粋関数による状態遷移、`assertNever` による網羅性
- **読者の状態**: 「型は通るのにランタイムで落ちる」を自分のコードで体験する

### 第2章: 「面接日程と面接官評価を保持したい」

- 降ってくる要件: 状態ごとに必要なデータが違うことが顕在化
- 引き出す原則: Discriminated Union の各バリアントへの固有フィールド、Companion Object パターン
- **読者の状態**: 状態に紐づくデータとロジックの一体管理を理解する

### 第3章: 「外部ATSから応募者を取り込みたい」

- 降ってくる要件: 外部APIの戻り値を `as JobApplication` していたら本番で `undefined` 参照で落ちた
- 引き出す原則: Zod スキーマによる境界防御、Always-Valid Domain Model、`as` から `safeParse` への移行
- **読者の状態**: kakehashi 記事が論じた「型検査の境界」を要件として体験する

### 第4章: 「応募者ID と求人ID を取り違えた」

- 降ってくる要件: 両方 `string` だったため引数の順序を間違えても型が通っていた
- 引き出す原則: Branded Type、`as` をブランド型ファクトリ専用に閉じる
- **読者の状態**: 構造的部分型の限界を ID という最小単位で体感する

### 第5章: 「エラー処理が `if/throw` で破綻している」

- 降ってくる要件: バリデーション失敗・状態遷移違反・外部依存エラーが入り乱れ、try/catch のネストが手に負えない
- 引き出す原則: Result 型、Railway Oriented Programming、ドメイン層からの `throw` 排除
- **読者の状態**: エラーをデータとして扱う発想を獲得する

### 第6章: 「ログから応募者のメールアドレスが流出した」

- 降ってくる要件: 教材内インシデント
- 引き出す原則: 構造的部分型がなぜPIIを型レベルで止められないか、Sensitive 型 + ログredactによる多層防御
- **読者の状態**: 型の限界を認めた上で、仕組みで防ぐ設計を獲得する
- **既存ブログ記事との接続**: 詳細は「ログのPII漏洩を防止する」記事へ送り、本書では要点と本書題材における適用に絞る

### 最終章: ふりかえりと、原則を継続適用するために

- 完成形のコードを第0章のコードと並べ、章ごとに何が変わったかを振り返る
- `functional-ts-principles` スキルプラグインを紹介し、エージェント時代に原則を継続適用する手段として接続する
- Phase 2 で扱う範囲（ドメインイベント、レイヤードアーキテクチャ）の予告

## 章内テンプレート

各章を同じリズムで読めるよう、6セクション構成とする。

1. **降ってくる要件** — PdM/QA/インシデント などからの依頼を自然言語で提示。読者が「自分ならどう直すか」を考える時間を持つ
2. **既存コードを動かしてみる** — 章開始タグを `git checkout` してテストを実行し、要件追加前のコードがどう動くかを確認する
3. **何が痛いのか** — 既存コードに要件を素朴に足してみると何が起きるか。型は通るのにランタイムで落ちる、変更箇所が散らばる、テストが書けない、などの痛みを言語化する
4. **どう直すか — 原則の紹介** — その章で導入する原則を最小限の例で説明。詳細は外部リンクに送り、本書では「この題材で何が解決されるか」に絞る
5. **リファクタを進める** — 段階的にコードを書き換える。途中の型エラー・テスト結果を交えながら進める
6. **章末の姿と次章への布石** — `git checkout` で章末タグの状態を確認できる。終わりに「次章で扱う痛み」を1段落だけ予告する

## コード管理方針

### リポジトリ構成

kosui.me リポジトリとは別の独立したリポジトリでコード本体を管理する。

- 仮リポジトリ名: `iwasa-kosui/growing-ts-apps-job-application`（決定は実装フェーズ）
- スタック: pnpm + TypeScript + Vitest + Hono + Drizzle（あるいは PostgreSQL + 軽量クエリビルダ）
- 外部依存はモック可能な構成にし、Docker などローカル実行のハードルを最小化する

### Git tag 運用

各章の開始状態と終了状態を Git tag で固定する。

- `step-00-start`, `step-00-end`, `step-01-start`, `step-01-end`, ... `step-final`
- 章本文中で `git checkout step-NN-start` を案内
- 各章末の差分は `git diff step-NN-start..step-NN-end` で見られる

### step ごとの README

各 step の README に、章のあらすじ、テストの動かし方、要件の再掲を載せる。リポジトリ単独で（kosui.me 本文を読まずに）完走可能な状態を維持する。

### 修正運用

タグを誤字や説明不足のたびに打ち直すと本文と乖離する。これを防ぐため次の運用とする。

- kosui.me 本文では `step-NN-end` タグの最新版を参照する前提で書く
- 章ごとの大きな書き換えは CHANGELOG で管理する
- タグを再作成（force update）する場合は CHANGELOG に記録する

## kosui-me 側の実装作業

実装は spec 承認後に別 PR で進める。spec 段階ではスコープのみを定義する。

- `apps/kosui-me/src/content.config.ts` に `books` コレクションを追加
- `apps/kosui-me/src/content/books/job-application/` 配下に冊メタ情報と各章 MDX を配置
- `apps/kosui-me/src/pages/books/index.astro` でシリーズ・冊一覧を表示
- `apps/kosui-me/src/pages/books/[book]/index.astro` で冊トップ（はじめに・目次）を表示
- `apps/kosui-me/src/pages/books/[book]/[chapter].astro` で各章を表示
- `apps/kosui-me/src/layouts/BookLayout.astro` を新設し、サイドバー付き2カラムを実装
- `apps/kosui-me/src/components/BookSidebar.astro` で章一覧を表示
- 既存の検索（Pagefind）に books を含めるかは別途判断（含める場合は `data-pagefind-body` の付与）

## Phase 2 として残す範囲

第1冊を完走させた後、同じ題材を継続して育てる形で次の章を執筆する余地を残す。

- **第7章: 採用監査で全状態遷移の履歴が必要になった** — ドメインイベントの設計
- **第8章: ユースケースが10個に増えてアプリ層が肥大化した** — レイヤードアーキテクチャと Result の使い分け（result-rop ルール）

別題材の第2冊（注文/予約/SaaS招待など）はシリーズ「育てるTypeScriptアプリケーション」の枠で別途企画する。

## リスクと懸念

- **第0章の初期コードのリアリティ**: 「他言語出身者が TS で書きそうな、ありがちなコード」へのチューニングが教材全体の印象を決める。執筆前にレビューを通す
- **第6章のPII章と既存ブログ記事の重複**: 詳細は記事に送り、本書では本書題材への適用と本質に絞る
- **章末タグと本文の乖離**: 上記「修正運用」で吸収する
- **完走率**: Phase 1 で8章。各章 30〜45分程度の読了時間を目安に、本文の長さを設計する
- **コードリポジトリの運用負荷**: kosui.me 本文と独立リポジトリの両方を保守する必要がある。初期は更新頻度を抑え、Phase 1 完成までは「本文を書きながら章末タグを打つ」運用とする

## オープン論点

spec 承認後、実装計画フェーズで詰める。

- コードリポジトリの正式名と公開リポジトリ/プライベートの判断
- スタック選定の確定（Hono / Drizzle 以外の候補も比較）
- BookLayout のデザイン（kosui.me の既存クレイモーフィズムとの整合）
- サイドバー進捗表示の有無
- Pagefind による books の検索インデックス化
- OG 画像の作成方針（章ごとに作るか、冊単位か）
- 第0章の初期コードのドラフト（執筆開始の最初のマイルストーン）
