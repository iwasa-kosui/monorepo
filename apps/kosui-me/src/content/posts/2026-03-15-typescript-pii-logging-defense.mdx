---
title: "TypeScriptの型だけでPII漏洩は防げない — ログ出力境界の多層防御設計"
date: "2026-03-15T12:00:00+09:00"
slug: "posts/2026/03/15/typescript-pii-logging-defense"
ogIcon: "ts"
description: "TypeScriptの構造的部分型はログへのPII混入を型レベルで防げない。型の限界を認めた上で、センシティブな値を関数クロージャに閉じ込めるSensitive型と、Pino redactによる多層防御の設計パターンを提案する。"
tags: ["TypeScript", "セキュリティ"]
private: false
---

## ログにPIIが混入するとなぜ致命的か

サーバサイド開発において、ログにPIIが混入する事故は珍しくありません。ユーザーのメールアドレスや氏名がログに書き込まれ、それがDatadogやNew Relicなどの外部SaaSに送信される。多くの開発チームにとっては「気をつけましょう」で済む問題かもしれません。

しかし、医療システムや金融システムのように厳格なデータ保護要件を持つドメインでは話が変わります。個人情報保護法やGDPRでは個人データの取り扱いに厳しい制約があり、ログ基盤として国外にデータセンターを持つSaaSを利用している場合、ログへのPII混入は個人データの国外移転に該当しうる。医療情報を扱う場合はHIPAAによるさらに厳しい制約も加わります。いずれにせよ、単なるバグではなくインシデントです。

厄介なのは、QAプロセスでログの中身が見落とされやすいことです。品質管理ではUIの使いやすさやAPIレスポンスの正しさに意識が向きがちで、「ロガーに何が渡されているか」を毎リリースで検証するチームは少ないのではないでしょうか。リリースのたびに全ログ出力をチェックするのは現実的ではないし、人手のチェックリストではスケールしません。

だからこそ仕組みで防ぐ必要がある。TypeScriptで型を使えば防げそうな気がしますが、実はそう簡単ではありません。

## 構造的部分型がPIIを透過させる

TypeScriptは構造的部分型を採用しています。これは、型の互換性を名前ではなく構造で判定する仕組みです。ある型が求めるプロパティをすべて持っていれば、余分なプロパティがあっても互換とみなされます。オブジェクトリテラルを直接代入する場合に限り、例外的に余剰プロパティチェックが働きますが、変数経由やスプレッド構文ではこの例外は適用されません。

この仕様がログへのPII混入を引き起こす具体的なシナリオを見てみましょう。

```typescript
type User = {
  id: string;
  role: string;
  email: string; // PII
};

type LogPayload = {
  id: string;
  role: string;
};

function logUserAction(action: string, payload: LogPayload): void {
  logger.info({ action, ...payload });
}
```

`logUserAction`は`LogPayload`だけを受け付けるので安全に見えます。しかし、`User`は`LogPayload`が求めるプロパティを全て持っているため、構造的部分型の互換性により`User`をそのまま渡せてしまいます。

```typescript
const user: User = { id: "1", role: "admin", email: "alice@example.com" };

// 型エラーにならない。Userは{id, role}を持っているので、LogPayloadとして互換
logUserAction("login", user);
// => {"action":"login","id":"1","role":"admin","email":"alice@example.com"}
```

この問題はリファクタリング時に特に起きやすい。最初は`LogPayload`専用のオブジェクトを`{ id: user.id, role: user.role }`のように明示的に構築していたのが、「`User`はどうせ`id`と`role`を持っているのだから直接渡せばいい」と誰かがショートカットした瞬間に、`email`が静かにログに混入し始めます。後から`User`に`phoneNumber`フィールドが追加されても、型エラーは出ません。

余剰プロパティチェックはオブジェクトリテラルの直接代入時にのみ例外的に発動する仕組みであり、変数やスプレッドを経由すれば効きません。スプレッド構文内のインラインオブジェクトリテラルでも余剰プロパティチェックを効かせてほしいという[要望](https://github.com/microsoft/TypeScript/issues/39998)はありますが、現時点では未対応です。

## Branded Typeで防げるケースと防げないケース

構造的部分型による漏洩を型レベルで防げないか。Branded Typeを使ったアプローチを考えてみます。

```typescript
declare const sensitiveTag: unique symbol;
type Sensitive<T> = T & { readonly [sensitiveTag]: true };

type User = {
  id: string;
  role: string;
  email: Sensitive<string>;
};
```

`Sensitive<string>`と`string`は型レベルで区別できます。これを活かして、センシティブなフィールドを含む型をロガーの引数型で拒否することを試みます。

**検出できるケース**: ロガーの引数型を`Record<string, string | number | boolean>`のようにプリミティブ型に制約すれば、`Sensitive<string>`（intersection型）を持つオブジェクトを直接渡そうとしたときに型エラーになります。

```typescript
function safeLog(data: Record<string, string | number | boolean>): void {
  logger.info(data);
}

// 型エラー: emailがSensitive<string>なのでstring | number | booleanに合わない
safeLog({ id: "1", role: "admin", email: user.email });
```

**検出できないケース**: 変数経由で渡すと、構造的部分型の互換性により通過します。

```typescript
const payload = { id: user.id, role: user.role, email: user.email };
safeLog(payload); // 型エラーにならない
```

さらに根本的な問題として、Branded Typeはコンパイル時だけの概念であり、ランタイムには何も残りません。`JSON.stringify`の出力に影響を与えることはなく、値は素の`string`としてそのままシリアライズされます。

これらの限界は個別のテクニックの問題ではなく、TypeScriptの設計思想に起因しています。[TypeScript Design Goals](https://github.com/Microsoft/TypeScript/wiki/TypeScript-Design-Goals)のNon-goal #3に「Apply a sound or "provably correct" type system. Instead, strike a balance between correctness and productivity.」と明記されている通り、TypeScriptは意図的に完全なSound型システムではありません。型検査が通ったコードがランタイムで型通りに動く保証はない。

型レベルの防御は「ないよりはまし」だが、すり抜けるパターンが構造的に存在し、すり抜けたときにランタイムでは何も守ってくれない以上、主防御にはなりえません。

## 値をシリアライズ不能にする: Sensitive型

ここまでの議論を振り返ると、問題の構造が見えてきます。

- **型レベルの防御**: コンパイル時のみ。ランタイムには何も残らない
- **ログ出力前のフィルタ（Zod strip等）**: ランタイムで機能するが、呼び忘れたら無防備

発想を変えて、**値そのものをシリアライズ不能にする**アプローチを考えます。

```typescript
type Sensitive<T> = {
  unwrap(): T;
  toJSON(): string;
};

function sensitive<T>(value: T): Sensitive<T> {
  return {
    unwrap() { return value; },
    toJSON() { return "[REDACTED]"; },
  };
}
```

`sensitive()`は値を関数クロージャに閉じ込めます。外から見えるのは`unwrap`と`toJSON`という関数プロパティだけであり、値自体はクロージャの中に隠されています。`toJSON()`を定義しているため、`JSON.stringify`でシリアライズされた場合は`"[REDACTED]"`が返ります。

```typescript
type User = {
  id: string;
  role: string;
  email: Sensitive<string>;
};

const user: User = {
  id: "1",
  role: "admin",
  email: sensitive("alice@example.com"),
};

// スプレッドしてログに渡しても、emailの値は漏れない
JSON.stringify({ ...user });
// => {"id":"1","role":"admin","email":"[REDACTED]"}

// 構造化ロガー（Pino等）でも同様
logger.info(user);
// => {"id":"1","role":"admin","email":"[REDACTED]"}

// console.logでも値は見えない
console.log(user);
// => { id: '1', role: 'admin', email: { unwrap: [Function: unwrap], toJSON: [Function: toJSON] } }

// テンプレートリテラルでも漏れない
console.log(`User: ${user.email}`);
// => "User: [object Object]"

// 値が必要な場面では明示的にunwrapする
sendEmail(user.email.unwrap());
```

このアプローチの決定的な強みは、**防御がデータ構造自体に組み込まれている**ことです。Zod stripのような「ログ出力前に毎回呼ぶ」アプローチでは呼び忘れが致命的ですが、Sensitive型は値をラップした時点で防御が成立します。出口で何もしなくても漏れない。

また、Branded Typeとは違い、ランタイムに実体を持つため、型検査をすり抜けてもシリアライズ時に値が漏れることはありません。ログ出力では`"[REDACTED]"`と表示されるため、「意図的にマスクされている」ことが明確に伝わり、障害調査で空オブジェクトと誤認されることもありません。

### Sensitive型が防げないケース

Sensitive型は万能ではありません。`unwrap()`で取り出した値は素の文字列に戻るため、取り出し後の扱いは開発者の責任です。

```typescript
// メール送信のためにunwrapした値を、うっかりログに含めてしまう
const rawEmail = user.email.unwrap();
logger.info({ action: "send_email", to: rawEmail }); // 漏れる
```

Sensitive型が守るのは「ラップされた状態の値」であり、「unwrapされた後の値」ではありません。この弱点を補うには、`unwrap()`の呼び出し箇所をESLintカスタムルールで制限し、「PIIを取り出せるコンテキストを限定する」といった運用上の対策を組み合わせる必要があります。

### ドメインモデルへの組み込み

Sensitive型は、PIIフィールドをドメインモデルの定義段階で明示する設計パターンです。

```typescript
// PIIフィールドが型から読み取れる
type Patient = {
  id: string;
  department: string;
  name: Sensitive<string>;
  diagnosis: Sensitive<string>;
  insuranceNumber: Sensitive<string>;
};

// 値が必要な場面では明示的にunwrapする
function formatInsuranceClaim(patient: Patient) {
  return {
    patientName: patient.name.unwrap(),
    diagnosis: patient.diagnosis.unwrap(),
    insuranceNo: patient.insuranceNumber.unwrap(),
  };
}
```

`unwrap()`の呼び出しはコードレビューで目立つため、「ここでPIIを取り出しているが、この用途は妥当か？」という判断を促す効果もあります。

## Pino redactとの併用

Sensitive型は強力ですが、全てのPIIフィールドに`sensitive()`を付け忘れなく適用できるかという問題は残ります。ここで[Pino](https://github.com/pinojs/pino)のredactオプションがフォールバックとして機能します。

```typescript
import pino from "pino";

const logger = pino({
  redact: {
    paths: ["email", "*.email", "password", "*.password"],
    censor: "[REDACTED]",
  },
});
```

Pino redactはブラックリスト方式であり、既知のセンシティブなフィールド名を指定してマスキングします。内部実装の[fast-redact](https://github.com/davidmarkclements/fast-redact)は`**.email`のような任意深度の再帰パターンをサポートしないため、ネストの深い構造では防御が不完全になりえます。新しいセンシティブフィールドが追加されたときにredactリストの更新を忘れるリスクもある。

だからこそ、Pino redactは主防御ではなく安全網です。

- **主防御**: Sensitive型で値をクロージャに閉じ込める。シリアライズ時に自動で不可視になる
- **安全網**: Pino redactで既知のセンシティブフィールド名をマスキングする。Sensitive型の適用漏れがあっても、フィールド名が既知であればキャッチできる

## まとめ

TypeScriptの構造的部分型は、余分なプロパティを持つオブジェクトを互換とみなします。Branded TypeやConditional Typesで型レベルの防御を試みることはできますが、変数経由で渡されると検出できず、そもそもランタイムには何も残りません。

この限界を踏まえて、以下の多層防御を提案しました。

1. **Sensitive型**: PIIを関数クロージャに閉じ込める。`JSON.stringify`、構造化ロガー、`console.log`のいずれの経路でも値は`"[REDACTED]"`として出力される。防御がデータ構造自体に組み込まれているため、「呼び忘れ」が存在しない。ただし`unwrap()`後の値の管理は開発者の責任であり、ESLintカスタムルール等で呼び出し箇所を制限する運用が望ましい
2. **Pino redact**: 既知のセンシティブフィールド名をブラックリスト方式でマスキングする。Sensitive型の適用漏れに対する安全網

「型安全な開発」という言葉に安心せず、型にできることとできないことを正確に理解すること。それがサーバサイドTypeScriptで堅牢なPII防御を設計する第一歩です。

## 参考文献

- [TypeScript: Type Compatibility](https://www.typescriptlang.org/docs/handbook/type-compatibility.html) — 構造的部分型と余剰プロパティチェックの仕様
- [Suggestion: perform excess property checks when spreading an inline object literal (TypeScript Issue #39998)](https://github.com/microsoft/TypeScript/issues/39998) — スプレッド構文で余剰プロパティチェックを効かせる要望（未対応）
- [TypeScript Design Goals](https://github.com/Microsoft/TypeScript/wiki/TypeScript-Design-Goals) — TypeScriptが意図的にSound型システムではないことの公式見解
- [Pino Redaction](https://github.com/pinojs/pino/blob/main/docs/redaction.md) — センシティブフィールドの自動マスキング
- [fast-redact](https://github.com/davidmarkclements/fast-redact) — Pino redactの内部実装。パスパターンの制約
