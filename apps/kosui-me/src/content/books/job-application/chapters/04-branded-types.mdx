---
bookSlug: "job-application"
chapterSlug: "04-branded-types"
order: 4
title: "「応募者ID と求人ID を取り違えた」"
summary: "両方 string だったため引数の順序を間違えても型が通っていた事故を起点に、Branded Type を導入します。"
codeTag: "step-04-end"
draft: true
---

## 降ってくる要件

「応募者ID と求人ID を取り違える事故が起きた。両方 `string` だったため引数の順序を間違えても型が通っていた」という要件が降ってきます。

## 既存コードを動かしてみる

```bash
git checkout step-04-start
pnpm test
```

## 素朴にエージェントに依頼してみる

本章は執筆中です。

## 何が痛いのか

構造的部分型は同じ形を持つ別の概念を区別できません。ID という最小単位でその限界が顕在化します。

## どう直すか — 原則の紹介

本章で導入する原則:

- Branded Type
- `as` をブランド型ファクトリ専用に閉じる

## リファクタを進める

本章は執筆中です。

## 章末の姿と次章への布石

次章では「エラー処理が if/throw で破綻している」という要件を起点に、Result 型と Railway Oriented Programming を導入します。
